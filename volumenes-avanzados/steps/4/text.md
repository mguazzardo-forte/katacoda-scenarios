Ahora vemos si el PV/PVC estan boundeados.

Lo  haremos con los siguientes comandos : `oc get pvc`{{execute}}

```NAME        LABELS    STATUS    VOLUME
claim1      map[]     Bound     volume1```

**Note:** Esto puede demorarse
